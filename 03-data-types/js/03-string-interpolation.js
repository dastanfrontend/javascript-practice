const greeting = 'Привет'
const name = "Александр"
const goodbye = `Пока`

const message = `${greeting}, ${name}`

console.log(message)

const a = 5
const b = 10
const sum = `Сумма a и b равна ${a + b}`

console.log(sum)
